# Bundled SparseDiffEngine version

Returns the version string of the SparseDiffEngine C library bundled
with this package.

## Usage

``` r
engine_version()
```

## Value

A character scalar, e.g. `"0.3.0"`.

## Examples

``` r
engine_version()
#> [1] "0.3.0"
```
